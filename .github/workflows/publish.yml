name: publish

# Publica ao criar um Release no GitHub. Trusted Publishing (OIDC) dispensa
# token de API: a autenticacao vem do proprio workflow.
on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar o pacote com as dependencias de desenvolvimento
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" build twine

      # Publicar uma versao quebrada e irreversivel: o numero fica queimado.
      - name: Rodar os testes antes de empacotar
        run: pytest -v

      - name: Construir sdist e wheel
        run: |
          rm -rf dist
          python -m build

      - name: Validar os artefatos
        run: twine check dist/*

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    # O nome precisa bater com o "Environment name" do trusted publisher no PyPI.
    environment: pypi
    permissions:
      # Exigido pelo OIDC: e o que substitui o token de API.
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - uses: pypa/gh-action-pypi-publish@release/v1
